# API
# Note: pydantic pinned to 2.11.7 because supabase 2.9.x (storage3/realtime)
# requires >=2.11.7. fastapi 0.115.0 is compatible with it.
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.11.7
pydantic-settings==2.5.2

# Database
# 2.31.0+: accepts Supabase's new sb_secret_/sb_publishable_ API key format.
# 2.9.x rejected them at create_client (JWT-shape regex). Compatible w/ pydantic 2.11.7.
supabase==2.31.0

# Reddit (no-auth: old.reddit HTML scraping — Reddit killed the free JSON API,
# and script-app OAuth is now gated on a moderation use case we don't have.
# Provider swap (Apify/etc.) planned for scale. See services/reddit.py.)
beautifulsoup4==4.12.3
lxml==5.3.0

# LLM (OpenRouter is OpenAI-compatible)
openai==1.54.0

# HTTP
httpx==0.27.2
requests==2.32.3

# Payments — x402 protocol (Coinbase SDK) + wallet signing.
# NOTE: eth-account pulls in bitarray, whose C extension Windows Smart App
# Control blocks. After ANY venv rebuild, re-copy infra/bitarray_stub/bitarray/
# over .venv/Lib/site-packages/bitarray/ or wallet signing breaks.
x402==2.15.0
eth-account==0.13.7

# Card issuance (Stripe Issuing test mode; StraitsX adapter swaps in at event)
stripe==12.4.0

# Browser automation — e-commerce discovery + checkout executor.
# After install run: playwright install chromium
playwright==1.54.0

# Utilities
python-dotenv==1.0.1
tenacity==9.0.0

# Dev / test
pytest==8.3.3
pytest-asyncio==0.24.0
ruff==0.7.1

# Amazon Bedrock Guardrails — the safety boundary around the agent (input,
# external content, and model output). Runtime only: no Bedrock foundation
# model is invoked, and credentials come from boto3's own provider chain.
boto3==1.43.71
