[Unit]
Description=laeria.ai monitor worker (Mode 3)
After=network.target

[Service]
Type=simple
# Edit User and paths for your VPS (hermes-donk).
User=CHANGE_ME
WorkingDirectory=/opt/laeria.ai/backend
Environment=PYTHONPATH=/opt/laeria.ai/backend
ExecStart=/opt/laeria.ai/backend/.venv/bin/python -m workers.monitor_worker
Restart=always
RestartSec=30
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target

# Install:
#   sudo cp laeria-monitor.service /etc/systemd/system/
#   sudo systemctl daemon-reload
#   sudo systemctl enable --now laeria-monitor
# Logs:
#   journalctl -u laeria-monitor -f
